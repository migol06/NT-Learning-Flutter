import 'package:flutter/material.dart';

class MyStarBody extends StatelessWidget {
  const MyStarBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Star Screen', style: TextStyle(fontSize: 50)),
    );
  }
}
